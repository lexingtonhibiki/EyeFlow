# EyeFlow

Windows 桌面上的上下文感知护眼提醒工具：在合适的时机、用合适的强度，请用户把视线从屏幕移开。本文件是项目的统一语言词汇表，只定义概念，不描述实现。

## Language

### 休息与提醒

**提醒（Reminder）**：
一次完整的护眼请求周期，由预告、休息、结算三个阶段构成。
_Avoid_：通知、弹窗、闹钟

**短休息（Short break）**：
按固定节奏安排的远眺休息，默认每 20 分钟一次、持续 30 秒（依据 AOA 20-20-20 法则，见 ADR-0001）。
_Avoid_：微休息、小休、mini break

**长休息（Long break）**：
连续用屏累计 2 小时后安排的一次 15 分钟休息（依据 AOA）。
_Avoid_：大休、rest break

**预告（Heads-up）**：
休息开始前若干秒的温和提示，让用户收尾；此时可延后、跳过或立即开始。
_Avoid_：预通知、倒计时提示

**休息界面（Break panel）**：
休息进行中展示倒计时与护眼贴士的置顶浮窗，默认不抢焦点、不遮全屏。
_Avoid_：遮罩、锁屏、overlay

**严格模式（Strict mode）**：
可选的全屏遮罩式休息形态，默认关闭。
_Avoid_：强制模式、锁定模式

**护眼贴士（Tip）**：
休息界面上展示的一条来自权威来源的短句（如眨眼、远眺 6 米）。
_Avoid_：文案、鸡汤

### 用户对提醒的动作

**完成（Complete）**：
休息倒计时走完，或用户确认已休息。
_Avoid_：结束、关闭

**跳过（Skip）**：
用户放弃本次休息，本次提醒周期立即结算。
_Avoid_：忽略、取消、dismiss

**延后（Postpone）**：
用户主动把当前提醒推迟固定分钟数，每次提醒最多一次。
_Avoid_：推迟、稍后、snooze、顺延

**暂停（Pause）**：
用户主动让整个提醒功能停止一段时间（如 1 小时、到明天）。
_Avoid_：静音、关闭提醒、禁用

**静音（Mute）**：
用户关闭提示音，视觉提醒照常。
_Avoid_：暂停、勿扰

### 上下文感知

**上下文状态（Context state）**：
系统对用户当下处境的判断，取值为桌面、心流、游戏、离开之一。
_Avoid_：模式、场景

**桌面（Desktop）**：
默认状态，正常投递提醒（声音 + 预告 + 休息界面）。
_Avoid_：普通模式、正常

**心流（Flow）**：
键盘持续高频输入的专注状态；提醒顺延到输入停歇后再投递。
_Avoid_：专注模式、打字中

**游戏（Gaming）**：
前台应用处于全屏或独占显示；视觉提醒顺延，仅以声音提示。
_Avoid_：全屏模式、沉浸

**离开（Away）**：
用户在阈值时间内无任何输入；计时暂停。
_Avoid_：空闲、闲置、idle、AFK

**顺延（Defer）**：
系统因上下文（心流、游戏）自动把提醒的投递推后，而不是丢弃。区别于用户主动的“延后”。
_Avoid_：延后、吞掉、跳过

**补发（Catch-up）**：
被顺延的提醒在上下文恢复为桌面后立即投递。
_Avoid_：重发、重试

**免打扰时段（Quiet hours）**：
用户设定的每天固定不提醒的时间范围。
_Avoid_：夜间模式、DND、勿扰

**可打扰性（Interruptibility）**：
操作系统给出的“此刻是否适合打扰用户”的判断（锁屏、演示、独占全屏等都视为不可打扰）。
_Avoid_：全屏检测、勿扰状态

### 计时与统计

**提醒计时器（Reminder timer）**：
决定下一次短休息何时开始的倒计时；离开时暂停，休息结算后重新开始。
_Avoid_：调度器、间隔

**用屏累计（Screen-time accumulator）**：
自上一次长休息或长时间离开以来的连续活跃用屏时长，用于触发长休息。
_Avoid_：工作时长、在线时长

**坚持统计（Adherence stats）**：
本地记录的今日完成、跳过、延后次数与连续坚持天数。
_Avoid_：数据面板、成就、打卡
